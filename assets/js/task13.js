// String

//Napisat function kotorayz sravnivaet dlinu strok

function fun1(str1,str2){
    return str1.length > str2.length ? 1
    : str1.length < str2.length ? -1
    :0;
}
// razbit na massiv slov

function fun2(str){
    return str.split(' ');
}

// poluchit dlini slov

function fun3(str) {
    const strArray=str.split(' ');
}